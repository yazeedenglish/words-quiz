/* ============= Base layout ============= */

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

body {
  margin: 0;
  min-height: 100vh;
  display: flex;
  justify-content: center;   /* center the whole card group */
  align-items: flex-start;
  padding: 40px 16px;
  background: linear-gradient(135deg, #eef2ff, #e0f7fa);
}

.page-wrapper {
  display: flex;
  flex-direction: column;    /* logo on top, quiz under it */
  align-items: center;
  gap: 24px;
}

/* ============= Logo card ============= */

.logo-card {
  background: #ffffff;
  padding: 18px 22px;
  border-radius: 22px;
  box-shadow: 0 16px 40px rgba(15, 23, 42, 0.12);
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.logo {
  max-width: 120px;   /* change this to resize logo */
  height: auto;
  display: block;
}

/* ============= Quiz container ============= */

.quiz-container {
  background: #ffffff;
  padding: 24px 20px 20px;
  max-width: 640px;
  width: 100%;
  border-radius: 22px;
  box-shadow: 0 18px 45px rgba(15, 23, 42, 0.15);
}

/* Header */

.quiz-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12px;
  margin-bottom: 16px;
}

.quiz-title {
  font-size: 1.2rem;
  font-weight: 700;
  color: #00344d;
}

.quiz-progress {
  font-size: 0.9rem;
  color: #64748b;
}

.quiz-meta {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4px;
  font-size: 0.85rem;
}

.timer {
  font-weight: 600;
  color: #dc2626;
}

.lives {
  font-size: 1rem;
}

/* Score chip */

.score-chip {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 0.8rem;
  padding: 4px 10px;
  border-radius: 999px;
  background: #eff6ff;
  color: #1d4ed8;
  margin-bottom: 10px;
}

/* Question area */

.question {
  font-size: 1.35rem;
  font-weight: 600;
  color: #00344d;
  margin-bottom: 10px;
  text-align: center;
}

.subtitle {
  font-size: 0.85rem;
  color: #6b7280;
  margin-bottom: 14px;
}

/* Options */

.options {
  list-style: none;
  display: grid;
  gap: 10px;
  margin-bottom: 16px;
}

.option {
  border: 1px solid #e5e7eb;
  border-radius: 999px;
  padding: 10px 12px;
  cursor: pointer;
  font-size: 0.95rem;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: background 0.15s ease, border-color 0.15s ease, transform 0.08s ease;
}

.option:hover {
  border-color: #93c5fd;
  transform: translateY(-1px);
}

.option.selected {
  border-color: #2563eb;
  background: #eff6ff;
}

.option.correct {
  border-color: #16a34a;
  background: #ecfdf3;
}

.option.wrong {
  border-color: #dc2626;
  background: #fef2f2;
}

.option-label {
  background: #e5e7eb;
  border-radius: 999px;
  padding: 2px 8px;
  font-size: 0.75rem;
  font-weight: 600;
  color: #4b5563;
  flex-shrink: 0;
}

/* Feedback & explanation */

.feedback {
  min-height: 24px;
  font-size: 0.9rem;
  margin-bottom: 6px;
}

.feedback.correct {
  color: #166534;
}

.feedback.wrong {
  color: #b91c1c;
}

.explanation {
  font-size: 0.85rem;
  color: #4b5563;
  margin-bottom: 12px;
}

/* Footer */

.quiz-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 8px;
}

/* Buttons */

button {
  border: none;
  border-radius: 999px;
  padding: 9px 18px;
  font-size: 0.9rem;
  font-weight: 600;
  cursor: pointer;
  transition:
